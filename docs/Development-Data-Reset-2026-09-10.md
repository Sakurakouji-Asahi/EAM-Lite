# 开发环境业务数据清空记录

完成时间：2026-09-10 12:47（Asia/Shanghai）。范围仅限当前 `eam-lite-dev` 开发环境及 `eam_lite_dev` 数据库，正式版未改动。

## 授权来源与适用范围

用户在本次项目对话中要求“把我现在放在里面的编码规则、分类、资产、物品等等全部清除”。首次执行被自动审批拦截，尚未删除数据；随后已说明完整重置还涉及关联的财务、审计、操作历史和备份记录，用户明确回复“我授权”。本次按该完整范围清空，保留登录账号、密码、角色和加密备份文件。

这是针对本次开发数据的一次性授权，不构成以后自动清空数据或修改正式环境的授权。

## 完成结果

- 编码规则、编码段、已发编号、分类、资产、物品、库存与领退记录、财务资料与折旧计划、保养记录、导入与导出记录，以及公司、部门、人员、位置等基础资料均已清空。执行前有 3 项资产、1 种物品和 4 个附件。
- 旧审计、操作历史和备份元数据一并清空；原始记录保存在清空前的加密备份中。当前数据库仅新增 1 条本次重置的系统记录，便于追溯本次操作。
- 当前附件存储为空，4 个已备份文件已移除；上传临时目录没有遗留文件。备份存储及现有密钥保持原样。
- 3 个登录账号的记录、密码摘要、角色和系统权限数据指纹完全一致。员工绑定、部门授权范围随基础资料清空，需要重新配置。
- 软件功能、最新迁移和数据库保护保持不变。系统回到尚未配置公司的初始化状态；旧登录会话已清除，使用原账号重新登录即可。

## 备份与验证

清空前停止开发应用写入，生成并校验数据库及附件的加密备份，另存本地副本：

`var/development-data-reset-20260910/BKP-20260910124733-73520679.eambak`

SHA256：`effdf2b6ba44f4fe8d45f3569995ce029afeac1e5873b5ca853bb550c1fd7aa7`。本地副本与原包一致；附件移除前再次完成备份包校验。密钥未写入项目文档或执行输出。

在 PostgreSQL 上先执行清空并回滚，确认原记录、账号、角色与数据库约束恢复一致后，才提交实际清空。共检查 74 张业务及会话表，清空后除本次重置记录外均为空；保留表数据未改变，触发器、约束和运行账号保护未改变。未修改应用业务代码或表结构。

开发服务已重启。系统检查通过，无待执行迁移；健康、版本、登录页面返回 200，并在只读事务中验证原管理员可以打开首页和初始化页面。没有创建新的业务测试资料。

证据保存在 `var/development-data-reset-20260910/`：`plan.json`、`backup.json`、`before.json`、`rehearsal.json`、`apply-started.json`、`database-cleared.json`、`storage-cleared.json`、`verification.json`。此前仅做准备的另一份加密备份也继续保留。

如需恢复清空前资料，应使用现有受控备份恢复流程，先在隔离目标验证备份，再按届时明确授权处理开发库；不能直接覆盖后来新增的数据。本轮验证了事务回滚和备份包完整性，没有将该备份完整还原到另一数据库。

## 后续使用

打开 [开发版初始化页面](http://127.0.0.1:8766/setup/)，使用原有管理员账号登录，从公司、部门、人员、分类、位置、编码规则等重新配置。已完成的“实物建档不等财务、照片可后补”功能继续保留。

后续状态：同日 17:10 已按用户提供的 [统一编码规则](Asset-Coding-Standard.md#本轮验证与运行状态) 更新开发版，保留重新录入的公司，建立默认编码和 10 个实物大类；尚需补充真实部门、人员、位置等初始化资料。该后续配置没有重复执行本页记载的数据清空。
