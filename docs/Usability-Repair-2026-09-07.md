# 资产管理需求与易用性修复记录

> **历史验证记录 · 2026-09-09**
> 下文结果限于记录的日期、代码和环境，不能自动认定当前工作树或生产环境已通过；原始证据继续保留。
> 当前协作依据见 [AGENTS.md](../AGENTS.md) 与 [文档定位和状态](README.md)。

日期：2026-09-07。范围：修复 2026-09-06 检测报告中的已确认问题及相关表单操作，不扩展新的 Sprint 或业务域。

## Completed

| 问题 | 修复结果 |
|---|---|
| 台账进入导出后丢失筛选 | 台账和导出共用筛选实现；新增 `/assets/export/` 预览，显示筛选摘要和行数，再调用原有受控 Excel 导出服务 |
| 台账筛选和列缺失 | 增加保养、标签、序列号、可见附件、建档日期、固定资产类别筛选，以及标签状态、最近可见盘点日期；低频筛选默认折叠 |
| 隐藏字段出现英文标题 | 隐藏字段保留提交值，不再渲染标题和必填星号；状态校验失败会提示刷新重试；资产变动历史改用中文状态 |
| 盘点发布前无法核对应盘范围 | 草稿详情和发布确认页显示预计应盘数及每页 25 项的清单；零项也明确显示；预览不写入快照，发布事务内重新校验并保存 |
| 部门、人员选择容易出错 | 新建、调拨表单按部门收窄责任人；切换部门会清除不匹配的人选；位置展示完整层级；后端仍独立校验 |
| 新建表单和盘点配置偏繁琐 | 新建资产的选填实物资料折叠；盘点类型与范围联动，仅显示适用字段；取消全部勾选不会复用旧隐藏选择 |
| 手机盘点入口不易理解 | 增加可展开的系统相机操作指引，保留现有扫码和登录权限机制 |
| 备份测试依赖执行顺序 | 改用会建立所需固定角色的既有测试工厂；前置测试之后执行真实备份恢复已通过 |

导出的公司和部门权限在后端重新执行，非法筛选不会扩大范围；筛选摘要也不能泄露越权部门、人员或位置名称。非财务用户的“有可见附件”筛选不计算仅财务可见的附件。当前台账导出沿用 `asset_ledger` 类型、幂等、审计和受保护文件下载；现有报表中心的历史日期口径及 T+ 对账保持原定义。

## Files Changed

- 资产筛选、导出和表单：`apps/assets/list_filters.py`、`form_options.py`、`forms.py`、`lifecycle_forms.py`、`models.py`、`views.py`、`urls.py`。
- 盘点预览与权限复核：`apps/inventory/services.py`、`views.py`、`forms.py`。
- 报表导出衔接与中文摘要：`apps/reports/queries.py`、`views.py`、`excel.py`。
- 导航名称：`apps/core/context_processors.py`。
- 页面：`templates/assets/` 中的台账、导出、草稿、详情、公共字段模板；`templates/inventory/` 中的草稿、详情、确认、预览、扫码指引和公共字段模板；`templates/base.html`。
- 新增本地表单联动脚本：`static/js/assignment-forms.js`。原 `app.js` 的重复提交保护保持原样。
- 回归测试：`tests/test_review_usability_fixes.py`、`tests/test_sprint12_backups.py`；同步 `tests/test_sprint3_http.py` 的中文文案断言。
- 文档：本记录、README。

## Database Migrations

没有新增数据库字段或迁移，也没有新增依赖。`manage.py check` 和 `makemigrations --check --dry-run` 通过。分批 PostgreSQL 测试从独立空库迁移，既有升级迁移测试也纳入回归。

## Tests

采用完整测试清单分批运行，并复测修复过程中的失败项及最后改动；不是一次不中断的单进程运行。最终对照当前 **2030 个唯一用例**，PostgreSQL 最新结果为 **2026 通过、4 个平台条件跳过**，无缺失用例和未解决失败。4 个 SQLite 条件分支已另行全部通过。

| 验证批次 | 结果 |
|---|---|
| 初始 PostgreSQL 完整清单分 4 批执行 | 2029 项；2017 通过、8 失败、4 条件跳过；原始失败日志保留 |
| 修正失败项及盘点、静态资源、备份、并发专项复核 | 59 通过，154.17 秒 |
| 最终筛选和权限复核 | 18 通过，76.22 秒 |
| SQLite 条件分支 | 4 通过，36.54 秒 |
| 最终非法筛选输入边界补查 | 1 通过，37.56 秒 |

初始 8 项失败分别来自：新增报表测试应使用独立事务的配置问题（5 项）、已改为中文的旧文案断言（1 项）、新增联动脚本与原静态资源断言的兼容问题（2 项）。已修正测试事务配置和文案断言，并将表单联动分成独立本地脚本，保留原重复提交保护测试。没有删除、跳过或放宽业务断言。

关键新增回归验证了：台账→预览→Excel 同范围、重复导出幂等、草稿号搜索、逐件低值资产视图、归档范围、可见附件筛选、跨公司和跨部门拒绝、隐藏字段保留、盘点预览无写入、发布前重新计算、伪造任务对象拒绝、有效盘点时间及取消全部勾选。

可核查证据在 Git 忽略目录 `var/repair-20260907/`：

- `verification-summary.json`、`verification-by-test.json`、`final-collection.json`：逐用例覆盖和最终结果汇总。
- `postgres-0.xml` 至 `postgres-3.xml`：完整分批原始结果。
- `final/pytest.xml`、`scope-final/pytest.xml`：失败修正和最终专项复核。
- `sqlite-branches.xml`、`boundary.xml`：SQLite 条件分支与输入边界。

浏览器在隔离模拟数据中实测：筛选 1 项后预览和生成文件仍为 1 项；新建及调拨的部门人员联动；选填资料折叠；盘点类型范围联动；草稿和发布确认页均显示 2 项清单；发布成功；中文状态历史。390×844 设置下，调拨和导出页面可用宽度与内容宽度均为 375 像素，无页面横向溢出；浏览器控制台未发现错误或警告。

## Business Rules Verified

- 公司和部门边界、角色与财务字段权限；越权筛选摘要不暴露对象名称。
- 正式资产数量固定为 1，财务确认、编码和标签生命周期继续沿用现有服务。
- 预览只读，正式发布与快照仍处于原有事务和锁定边界；重复扫描、停止扫码及关闭等并发回归通过。
- Excel 继续使用既有列类型、公式注入防护、幂等和审计机制。
- 真实加密数据库及附件备份恢复、低值物品并发编号专项通过。

## Not Implemented

没有同步正式版，没有新增网页内摄像头或连续扫码组件。真实手机、纸张标签、正式内网 HTTPS、独立备份介质及多角色现场验收仍按原上线门槛执行。

## Risks / Follow-up

修复已加载到现有开发服务。重载前后均为 1 个公司、1 项资产、2 个附件，现有数据库、附件和局域网配置保留。当前开发服务仍不等于正式生产上线批准。建议按岗位实际完成一次建档、调拨、盘点和筛选导出验收，再按既有发布流程同步正式版。
