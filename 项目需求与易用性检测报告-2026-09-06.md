# EAM-Lite 资产管理需求与易用性检测

> **历史检测记录 · 2026-09-09**
> 原始问题和测试结果按检测日期保留；后续处理见 [9 月 7 日修复记录](docs/Usability-Repair-2026-09-07.md)。引用行号对应当时版本，不应直接当成当前待办。
> 当前协作依据见 [AGENTS.md](AGENTS.md) 与 [文档定位和状态](docs/README.md)。

检测日期：2026-09-06。代码版本：v0.2.1，commit `7d211685a18486f0caf111417e6e0f5ea83d9b6f`。

结论：核心资产管理能力覆盖较完整，适合继续进行公司内部业务验收；仍存在已复现的功能衔接、易用性和测试稳定性问题，不能认定为全部需求验收通过或生产上线完成。日常查询和按待办处理有良好基础，新建、调拨、盘点发布等操作仍需简化。这个判断来自需求、源码、自动测试和模拟数据页面操作，不是员工真实使用满意度调查。

## Completed

### 需求覆盖判断

本次以 `AGENTS.md`、`docs/00`、`docs/01`、`docs/02`、`docs/04`、`docs/05`、`docs/07`、`docs/10` 及相关验收文档为依据。范围是企业内部逐件资产与批准的低值物品管理。

| 管理需要 | 检查结论 | 主要实现和证据 |
|---|---|---|
| 资产台账、分类、部门、责任人、层级位置 | 已实现，列表能力仍有差距 | `apps/assets`、`apps/masterdata`；实际打开台账、新建草稿、详情 |
| 正式编号、编号历史、单件管理 | 已实现相应业务服务和数据库保护 | `apps/coding`、`IssuedCode`；Sprint 2–4 编码、正式化与并发测试 |
| 财务认定与六种折旧方法 | 已实现独立财务模块 | `apps/finance`；金额 Decimal、方法规范例子、冲销与权限测试 |
| 二维码、标签、照片附件 | 已实现 | `qr_services`、标签打印与确认、受保护附件；真实手机和纸张仍需现场验收 |
| 调拨、借还、处置和历史 | 已实现 | 模拟页面成功调拨，当前归属更新并新增历史；其他动作纳入自动回归 |
| 资产盘点 | 已实现草稿、发布快照、扫码、差异与关闭 | 实测发布 2 项快照；随后调拨资产，快照仍保存原部门、责任人、位置 |
| 预防性保养、离职清退 | 已实现 | `apps/maintenance`、`apps/offboarding`；到期计算、权限、处置联动、未清阻断测试 |
| 低值物品 | 已实现独立数量管理模块 | `apps/supplies`；库存、领退调拨、保管、盘点、清退、报表测试 |
| Excel 导入导出与 T+ 对账 | 已实现，台账到报表的筛选衔接有缺陷 | `apps/imports`、`apps/reports`；T+ 为 Excel 人工对账 |
| 权限、审计、公司边界、备份 | 有后端实现和测试 | `permissions`、`apps/audit`、`apps/operations`；实际生产部署与恢复验收另计 |

这些功能的存在及测试覆盖不等于每条需求已经由业务负责人签字验收。

### 已复现的问题

**1. P2：从台账进入报表会丢失当前筛选，容易导出错误业务范围。**

- 重现：模拟公司有两个部门，各有 1 项正式资产。台账筛选第一个部门，显示“共 1 条”；点击“报表与导出”，报表显示“共 2 行”，部门恢复为“全部部门”。
- 原因：`templates/assets/asset_list.html:10` 的链接固定为 `/reports/?report_type=asset_ledger`，没有传递部门、责任人、状态等条件。报表也没有承接台账关键字和位置筛选的现成统一接口。
- 影响：用户必须重新选择范围，否则报表与刚才看到的台账不一致。这不是已证实的越权，仍在该账号获准范围内。
- 建议：建立台账与导出的统一筛选映射；在导出前显示有效筛选及行数。补充“两个部门各一项，筛选一个部门后导出仍只有一项”的回归。
- 证据：`var/review-20260906/ui/filter-reproduction.json`；浏览器中独立复现。需求见 `docs/05-UI-UX.md:165`。

**2. P2：调拨等动作页显示内部隐藏字段的英文标题和必填星号。**

- 重现：进入“资产调拨 / 转交”，页面可见 `Idempotency key`、`Expected status*`、`Expected department id*` 等标题，却没有相应输入框。
- 原因：`templates/assets/lifecycle_action.html:13` 遍历全部字段；`templates/assets/_field.html:10` 给隐藏字段同样渲染可见标签。相关隐藏字段定义位于 `apps/assets/lifecycle_forms.py:25`。
- 影响：员工会误以为缺少必须填写的内容，表单增加无意义空白；不符合中文、面向业务用户的要求。
- 建议：隐藏字段只输出隐藏控件；可见字段才生成标签和布局。历史中的 `pending_label → in_use` 等状态也应使用中文显示。
- 证据：浏览器可见性检查和截图均确认这些英文标题实际显示，并非只存在于页面源代码。

**3. P2：盘点发布前没有预计应盘数量与清单预览。**

- 重现：保存全公司盘点草稿后，详情仅显示范围、日期、执行人；发布确认页只给出通用说明和“我已核对影响范围”复选框。正式发布后才出现“应盘 2”。
- 原因：`apps/inventory/views.py:555` 在草稿状态不生成汇总；`templates/inventory/task_detail.html:16` 将清单限定在非草稿状态；`templates/inventory/confirm_action.html:3` 没有范围明细。
- 影响：用户无法在发布前核实漏选或多选，确认提示缺少可供核对的具体内容。
- 建议：发布前提供只读“预计 N 项”和清单预览；发布事务内仍重新校验并生成正式快照。
- 需求依据：`docs/05-UI-UX.md:490` 明确要求保存草稿后显示预计应盘数和快照预览。

**4. 需求差距：资产台账尚未达到文档规定的全部筛选和列表能力。**

- 当前台账有分类、部门、责任人、位置、状态、归档范围和财务角色可见的会计认定。
- 对照 `docs/05-UI-UX.md:170`、`:186`，台账本页缺少是否需要保养、初始化日期、标签状态、是否有序列号、是否有附件等筛选，也未显示标签状态和最近盘点列。
- 标签模块、详情或部分报表中已有相关信息，但不能替代台账内一次筛选和查看的要求。
- 建议：将低频筛选放入“更多筛选”，加入标签和最近盘点列，保留默认页面简洁。

### 易用性判断

已经做得较好的部分：中文工作台；导航按工作任务折叠；角色决定可见入口；“我的待办”；保存草稿后直达照片上传；调拨预填当前归属；明确的状态提示；正式数据保留历史。

建议在上述缺陷之外优先改善：

1. **部门与责任人联动。** 新建和调拨时，选择部门后人员列表仍包含其他部门员工。本次故意只修改目标部门，提交后得到“新责任人必须属于目标部门”，保护有效，但可以提前避免选错。
2. **减少首次录入负担。** 新建资产一次展开约 18 个标准字段；“说明”和“备注”同时出现。可将品牌、厂家、旧编号等选填项折叠，突出名称、分类、单位、归属和照片。财务确认、正式编号和逐项贴标的必要控制仍应保留。
3. **简化盘点配置。** 表单默认“部门盘点”配“全公司”，实际校验不允许这一组合；各类范围字段和勾选资产表同时展示。应联动类型与范围，只显示当前需要的选择。
4. **优化手机扫码指引。** 390×844 设置下，盘点入口页面测得可用宽度与内容宽度均为 375 像素，三个主要按钮高度均为 44 像素；资产详情亦无页面横向溢出。当前入口是扫码枪输入，或使用系统相机打开标签再进入本次盘点；不是网页内直接启动相机。可以明确提供“手机如何扫码”的短指引，并评估连续盘点是否需要更短的切换路径。

本次未让真实设备、财务和仓库人员完成计时任务，因此不提供主观的“易用性百分比”。建议以“独立建一项资产、完成一次调拨、盘点 10 项、按部门导出”做一轮现场操作验收，记录完成率、耗时和求助次数。

### 回归稳定性问题

**P2：真实备份恢复测试存在可复现的执行顺序依赖。**

全仓接续运行中，`test_real_encrypted_backup_restores_to_fresh_database_and_reconciles` 在准备低值物品期初库存时，因测试仓库用户缺少角色而被拒绝，尚未执行到备份步骤。它单独在新库中运行通过，但先运行 `test_current_local_restore_refuses_nonempty_database_without_overwrite` 再运行它，稳定复现 `1 passed, 1 failed`。

`tests/test_sprint12_backups.py:41` 使用的旧 `make_user` 位于 `tests/test_sprint1_services.py:28`，仅通过 `Group.objects.filter(name__in=roles)` 绑定已经存在的角色；事务测试清理数据后，这种写法会静默漏掉仓库角色。建议让测试夹具明确创建所需固定角色，或改用现有会创建角色的测试工厂，并保留顺序复现回归。本次未修改测试。

这说明回归验收目前不能稳定全绿；它不是已经证实的生产业务账号权限故障。

## Files Changed

- 新增本检测报告。
- `var/review-20260906/` 下为本次隔离测试脚本、日志和模拟界面数据，处于 Git 忽略目录。
- 未修改应用源码、既有测试或批准的需求文件。
- 未改动现有开发数据库或正式业务数据。

## Database Migrations

- 无新增业务迁移。
- `makemigrations --check --dry-run`：`No changes detected`。
- 自动回归使用本次新建的 PostgreSQL 18.6 临时容器，测试库与现有开发库隔离。
- 浏览器模拟使用独立 SQLite 文件；它仅用于页面流程复现，不作为并发或 PostgreSQL 约束验收依据。

## Tests

收集完整测试清单为 **2017 项**。长时间命令中断后，将首次进度日志与完整收集顺序逐项核对，从第 916 项开始，在新测试数据库接续。没有跳过未完成用例，也没有把中断进程记为成功退出。

| 批次 | 通过 | 失败 | 跳过 | 说明 |
|---|---:|---:|---:|---|
| PostgreSQL 首段 | 912 | 0 | 3 | 915 项有进度结果；进程中断，无该段最终退出码及 JUnit |
| PostgreSQL 接续 | 1099 | 2 | 1 | 1102 项，退出码 1，787.11 秒 |
| PostgreSQL 原始累计 | **2011** | **2** | **4** | 对应完整 2017 项清单；不能称为全量通过 |
| SQLite 条件分支补跑 | 4 | 0 | 0 | 45.75 秒；1 条 pytest 缓存写入警告，不影响断言 |
| 两项失败独立复查 | 2 | 0 | 0 | 新库、配置 Docker 备份工具，56.88 秒；包括真实加密备份及恢复 |
| 备份顺序复现 | 1 | 1 | 0 | 37.86 秒；确认上述测试夹具顺序依赖 |

原始两项失败的判断：

- **备份恢复用例：** 失败发生在测试数据准备的角色权限检查。单独成功、顺序运行再次失败，需修复测试夹具；不可用单独复测覆盖原始失败。
- **并发编号用例：** 原始报错为 Windows `10055 / No buffer space available`，连接临时 PostgreSQL 时失败。独立复查通过；本次没有复现重复编号，但长时间套件应在资源稳定的环境再次运行。

4 个条件跳过分别是两个旧标签 SQLite 夹具、一个 SQLite 位置升级夹具和一个 SQLite 报表读侧用例，均已在 SQLite 补跑通过。PostgreSQL 并发和约束用例没有被 SQLite 替代。

日志和结构化证据位于：

- `var/review-20260906/initial-results.json`、`resume-summary.json`、`pytest.log`；
- `var/review-20260906/pytest-remaining.xml`、`pytest-remaining.log`；
- `var/review-20260906/sqlite-branches.xml`；
- `var/review-20260906/diagnostics/pytest.xml`；
- `var/review-20260906/backup-order/pytest.xml`。

当前已完成的基础检查：

- `manage.py check`：0 issues。
- `manage.py makemigrations --check --dry-run`：无模型迁移漂移。
- `python -m pip check`：无损坏依赖。

页面实测：登录、首页、台账筛选、报表衔接、新建草稿、照片上传入口、调拨错误拒绝、调拨成功留痕、盘点草稿、发布确认、发布快照、手机盘点入口与资产详情布局。切换为单一设备角色后，财务概览不显示，直接访问资产财务地址返回“403 无权访问”。

本次页面操作使用内置浏览器及隔离模拟数据。现有 Edge 标签的自动化连接不可用，没有完成本轮独立 Edge 验收；未测试真实手机摄像头或纸张打印，也未以照片上传入口可达代替实际文件上传结果。

## Business Rules Verified

- 模拟界面实际验证：资产归属变更保留前后历史；部门与员工不匹配被拒绝；盘点发布后资产调拨不改变原快照；设备角色无权读取资产财务，直接访问也被拒绝。
- 已检查实现并纳入自动回归的重点：物理分类与财务认定分离、`Asset.quantity=1`、编号唯一与不复用、六种折旧、确认历史保留、角色及公司/部门范围、受保护附件、导入确认与幂等、导出权限与审计、库存和保管一致性。
- 按当前批准边界，T+ API 自动入账、采购、生产物料库存、MES、完整维修工单等属于范围外；不作为本次缺陷。

## Not Implemented

本次是检测，没有实施修复、需求扩展、正式环境部署或发布。没有将文档中尚未完成的人工验收认定为通过。

## Risks / Follow-up

1. **生产上线条件仍未闭合。** 当前实际可访问实例显示“开发环境”，运行 commit 与仓库一致；本机现有业务容器仅见开发应用和开发数据库。只读查询确认开发实例有 1 个公司、1 项在用资产、1 个启用部门、1 名启用员工、5 个启用位置，初始化已完成。这个数据量无法代表真实集中建账和盘点场景。`README.md:17` 与 `docs/Sprint-12-UAT-Evidence.md:89` 仍记录固定 DNS、受信任 HTTPS、独立备份、真实终端及多角色签字等门槛。本次没有发现足以证明它们全部关闭的现场证据。低值物品后续文档有独立 Edge/手机视口证据，不能据此替代整个系统的现场验收。
2. **先修正业务结果与界面误导，并关闭测试失败。** 优先处理台账导出筛选、隐藏字段标题、盘点预览和备份测试夹具；在稳定环境重跑完整回归，然后补台账能力及联动控件。
3. **用实际岗位完成验收。** 对约 150 项初始资产规模，现有架构和功能范围匹配；最终是否“简单易用”应由实际录入、设备、仓库、财务和盘点人员验证。

本报告记录当前观察与修复建议，不变更批准的业务规则，也不构成上线批准。
