# EAM-Lite 当前协作规则

生效：2026-09-09。用户已说明原业务文档由 AI 生成，并要求精简规则、将长文档降为参考，让开发围绕实际目标自主优化。本文件替代原 504 行规则；旧文见 [历史原文](docs/archive/AGENTS-before-2026-09-09.md)。

## 1. 需求与文档的地位

- 在项目资料中，以用户当前指令和有明确确认依据的业务决定为准，再按本文件开展工作。
- 长需求与设计稿是 AI 参考，文档编号不代表批准；`tasks/` 是历史 Sprint 任务。文中“approved”“已批准”“必须”“禁止”“唯一依据”等字样本身不证明用户逐条确认，也不自动形成当前任务授权或长期限制。
- 代码、测试、迁移说明当前实现；历史报告说明当时观察。两者都不能单独证明产品要求正确或当前验收通过。
- 开始工作按需阅读相关代码、测试和文档，无需每次通读整套 PRD。当前入口与已确认事项见 [文档索引](docs/README.md)。

## 2. 自主完成本次目标

- 先检查工作树与相关实现，保留用户已有修改；依据当前目录和分支工作，不因旧文档自动切分支、重新初始化项目或重做已实现的 Sprint。
- 以本次用户目标控制范围。用户明确指定某个 Sprint 时遵守该范围；普通维护和改进不受旧 Sprint 顺序、逐阶段等待或旧任务排除项的机械限制，也不自动启动整条历史路线。
- 页面、交互、字段展示、查询、校验、内部结构及必要的小范围重构，可以根据实际问题自主优化并验证，不因偏离 AI 草案重复请求确认。
- 优先利用项目现有结构与合适的工具；技术方案和依赖应服务当前目标，说明必要性并控制维护成本，不把旧技术选型当成永久禁令。
- 文档冲突先判断性质。普通实现差异可据当前目标处理并说明；只有缺少决定会改变金额结果、业务含义、权限范围，或涉及不可逆数据操作时，才在现有授权不足的情况下提出具体问题。继续完成不依赖该决定的工作。

## 3. 保护数据与正确性

- 保留已有业务数据、用户改动、身份关联、历史编号、已确认财务记录和审计。不能为适配新方案静默删除、重置、覆盖或改写历史。
- 改变已有数据含义或表结构时，说明兼容性并使用可追踪迁移；先验证迁移与恢复路径，不直接改生产库。当前单件 Asset 与数量型 supplies 的分离是已有数据契约，调整时须评估影响。
- 金额使用 Decimal；涉及计算变化时，用明确案例和精确断言验证舍入与历史影响，不把 AI 文档中的公式或金额阈值当作用户确认凭据。
- 权限在后端执行，维护公司、部门及敏感字段隔离；附件按权限访问，不暴露密码、密钥或真实凭据。调整权限应说明获得或失去访问权的对象。
- 关键业务动作保留事务、幂等和审计，通过受控服务更新状态及历史，避免只改当前值而破坏记录关联。
- 不为让测试通过而删除有效断言；明确区分既有失败、新引入问题和需要随需求调整的旧断言。

## 4. 验证与交付

- 按改动影响选择验证：纯文档检查内容、链接和一致性；业务逻辑运行相关自动测试，必要时运行完整回归；表结构变化检查迁移；权限变化覆盖拒绝和越界场景。
- PostgreSQL 锁与并发必须在 PostgreSQL 验证，不能用 SQLite 结果代替。未执行的测试明确说明，不沿用旧报告宣称本轮通过。
- 简明报告完成内容、修改文件、实际验证和剩余问题；涉及迁移、业务口径或部署时补充对应影响，无需所有小任务套用七段 Sprint 报告。
- 部署和数据操作按用户实际授权执行。代码完成、本机可用、历史 UAT 通过，都不自动代表真实生产环境已验收。

## 5. 维护文档

- 用户明确确认的新业务决定，记录日期、原意、适用范围及确认来源；AI 的建议和自行推断标为假设，不能自行标成已批准。
- 更新当前说明；保留历史任务和证据的原始语境，并链接后续处理。文档日期较新不等于优先级更高。
- 更详细的 AI 草案可用于查遗漏、理解旧设计和寻找测试案例；可以提出更好的方案，不需要为了服从草案牺牲可用性或增加无必要流程。
